import { makeStyles } from "@material-ui/core";
const useEditProductStyles = makeStyles({

    form_container: {
        marginTop: "100px",
        justifyContent: "space-around",
        alignItems: "center",
        display: "flex",
        flexDirection: "column",
      },

})

export default useEditProductStyles