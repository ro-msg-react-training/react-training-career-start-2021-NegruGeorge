import { makeStyles } from "@material-ui/core";

const useListProductStyles = makeStyles({
  listItems: {
    display: "flex",
    flexDirection: "row",
    marginTop: "100px",
    justifyContent: "center",
  },
});

export default useListProductStyles;
